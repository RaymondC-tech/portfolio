declare module 'orbit-space' {
    import { FC, PropsWithChildren } from 'react';
  
    /** 
     * The default export is a React component that
     * wraps your app’s content in the 3D background.
     */
    const OrbitSpace: FC<PropsWithChildren<{}>>;
    export default OrbitSpace;
  }
  